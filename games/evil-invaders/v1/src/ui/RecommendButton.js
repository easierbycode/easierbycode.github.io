import { ModeButton } from "./ModeButton.js";

export class RecommendButton extends ModeButton {
    constructor(defaultFrameName, downFrameName) {
        super(defaultFrameName, downFrameName);
    }
}

export default RecommendButton;
